# FlaskProject
A Flask-based web application built with a modular structure, including templates, static assets, and Python backend logic. 
This project demonstrates clean routing, template rendering, and integration of custom Python scripts such as `scheduling.py`
